import {
  Typography,
  AppBar,
  Toolbar,
  Menu,
  MenuItem,
  Container,
} from "@mui/material";

export default function Home() {
  return (
    <AppBar position="relative">
      <Toolbar>
        <Typography variant="h2" align="left">
          Kate Renae
        </Typography>
      </Toolbar>
    </AppBar>
  );
}
